<!DOCTYPE html>
<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <link href="https://fonts.googleapis.com/css?family=Lobster|Merienda" rel="stylesheet">
    <script src="/assets/content/show.js" type="text/javascript"></script>

    <title><%= content_for?(:title) ? yield(:title) : 'Searching For Party' %></title>

    <%= stylesheet_link_tag    'application' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

    <%= render 'layouts/navbar' %>
  </head>

  <body>
    <body onload="hidefield()">
    <div id='flash'>
      <% if notice %>
        <div class='alert alert-success'><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class='alert alert-danger'><%= alert %></div>
      <% end %>
    </div>
    <div id='universal'>
      <%= yield %>
    </div>
  </body>
</html>
